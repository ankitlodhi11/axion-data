apiVersion: v1
kind: Service
metadata:
  name: axion-telemetry-service
spec:
  type: LoadBalancer   # Agar direct UI pod/Ingress se connect ho raha hai
  selector:
    app: axion-telemetry   # Dhyaan dein: Ye label telemetry deployment ke template labels se match hona chahiye
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000